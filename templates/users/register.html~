{% extends "dashboard.html" %}

{% block nav_bar %}
    {% include "users/_nav_bar.html" %}
{% endblock %}


{% block body %}

<div id="register_box">
    <h3 id="register">
        注册     or
        <span id="login"><a href="">登录</a></span>
    </h3>
<div id="register_form">
    <form id="register" action="{% url 'user_register' %}" method="post">
        {% csrf_token %}
        <div class="form-group">
            <label for="school_id">用户名</label>
            <input type="text" id="school_id" name="school_id" placeholder="请输入学号">
        </div>
 	<div class="form-group">
            <label for="jw_password">教务密码</label>
            <input type="password" id="jw_password" name="jw_password" placeholder="请输入教务密码">
        </div>
	<div class="form-group">
            <label for="password">登录密码</label>
            <input type="password" name="password" id="password" placeholder="请输入登录密码">
        </div>
        <div class="form-group">
            <select name="pswquestion" id="pswquestion">
                <option selected="selected" value="" >请选择密码找回问题</option>
                <option value="1">A</option>
                <option value="2">B</option>
                <option value="3">c</option>
            </select>
        </div>
        <div class="form-group">
            <label for="pswanwser">问题答案</label>
            <input type="text" name="pswanwser" id="pswanwser" placeholder="请输入密码找回问题答案">
        </div>
        <div class="form-group">    
            <label for="captcha">验证码</label>
            <input type="captcha" id="captcha" name="captcha" placeholder="请输入验证码">
                <img src="data:image/jpeg;base64,{{ captcha }}" />
        </div>
            <input type="submit" id="submit" value="register">
        </form>
    </div>
</div>

{% endblock %}
